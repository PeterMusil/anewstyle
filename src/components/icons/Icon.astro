---
interface Props {
  name: string;
  iconClass?: string;
}

const { name, iconClass } = Astro.props;
---

<svg class={`icon icon-${name} ${iconClass}`}>
  <use xlink:href={`#icon-${name}`}></use>
</svg>
